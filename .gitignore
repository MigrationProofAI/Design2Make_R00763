# ── secrets — NEVER commit ───────────────────────────────────────────────
.env
.env.*
env.txt
*.pem
*.key
*.crt
*.p12

# ── SAP NWRFC SDK ─────────────────────────────────────────────────────────
# Bundled (mcp_remote/nwrfcsdk/) for the OWNER's own PRIVATE cross-machine use only.
# It is SAP-licensed and must NOT be redistributed — keep this repository PRIVATE.
# (This is the LINUX build: .so libs. A Windows host needs the Windows NWRFC SDK instead.)

# ── runtime data / regenerable (not source) ──────────────────────────────
logs/
vector_store/
tts_cache/
kg_instances.json
genesis_ledger.json
adk_sessions.db
*.db
*.db-journal
.adk/

# ── data dumps (regenerated from SAP; keep out of a public repo) ──────────
expand_*.json
code_book.json
dev_rfc.log

# ── python ───────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# ── node / vite (the served bundle in static_v2/ IS committed) ────────────
frontend/node_modules/
frontend/dist/
node_modules/

# ── misc ─────────────────────────────────────────────────────────────────
*.log
*.webm
_review_panel.png
.DS_Store

# learning loop (runtime data)
lessons.jsonl
promotions.jsonl
learning_ledger.jsonl
logs/learning_events.jsonl
